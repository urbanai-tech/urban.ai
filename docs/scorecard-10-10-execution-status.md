# Execução do scorecard 10/10

**Início:** 2026-07-15  
**Última validação operacional:** 2026-07-22
**Fonte de escopo:** `plano-mestre-scorecard-10-10-2026-07-15.md`  
**Estado:** gates locais e publicação canônica concluídos; produção com liveness/readiness, MySQL e Redis saudáveis; status page pública provisionada e em emissão de TLS; restore real recuperou o banco em 27 s, mas revelou trilha administrativa vazia; certificação 10/10 ainda depende de DNS de staging, fechamento de DR/auditabilidade, integrações reais, beta, SLO e aprovações humanas.

## Baseline revalidado

| Frente | Estado observado | Evidência |
|---|---|---|
| Repositório | `urbanai-tech/urban.ai` e `Gustavogm9/urban.ai` sincronizados em `main` no SHA `ca999392`; worktree limpo e branch local já contida na canônica | `git fetch`; `git rev-parse`; `git merge-base --is-ancestor`; `git status` em 2026-07-20 |
| CI canônico | CI, release gate, CodeQL e backups mais recentes verdes em `urbanai-tech/urban.ai` | `gh run list --repo urbanai-tech/urban.ai --branch main` em 2026-07-20 |
| CI fork | CI e release gate verdes; jobs operacionais agendados ficam `skipped` por guard intencional, pois os secrets vivem apenas no repositório canônico | `gh run list --repo Gustavogm9/urban.ai --branch main` em 2026-07-20 |
| Produção web | `myurbanai.com` e `app.myurbanai.com` respondem | smoke HTTP em 2026-07-15 |
| Liveness | `/health/live` responde 200 em produção | smoke HTTP em 2026-07-20 |
| Readiness | `/health` sem credencial responde 401; com Bearer autorizado responde 200 e `status=ok`, com DB e Redis em `ok` | smoke autenticado sem registrar o token em 2026-07-20 |
| Redis de produção | serviço `Redis-P9x7` em `SUCCESS`, 1/1 réplica, volume persistente `redis-volume-021V` em `/data` e credencial rotacionada | [`evidence/production-redis-readiness-2026-07-20.md`](evidence/production-redis-readiness-2026-07-20.md) |
| DNS operacional | `myurbanai.com`, `app.myurbanai.com` e `status.myurbanai.com` resolvem; a status page está construída, mas segue sem certificado customizado; staging e staging-api possuem CNAME e TXT únicos, porém os próprios destinos atualmente referenciados pelos CNAMEs retornam 404, enquanto o domínio nativo canônico do frontend responde 200; os três hostnames customizados falham em TLS estrito | [`evidence/staging-api-dns-2026-07-20.md`](evidence/staging-api-dns-2026-07-20.md), [`evidence/staging-frontend-2026-07-20.md`](evidence/staging-frontend-2026-07-20.md) e [`evidence/status-page-2026-07-20.md`](evidence/status-page-2026-07-20.md) |
| Acesso Railway | acesso foi validado antes das alterações em 2026-07-20; em 2026-07-22 a sessão expirou na CLI e no conector, bloqueando nova inspeção interna até `railway login` | inspeção operacional Railway em 2026-07-20 e tentativa read-only em 2026-07-22 |
| Backend local | 92 suítes e 896 testes verdes; lint e build sem erros; `npm audit --omit=dev` zerado | Jest completo; ESLint 10; Nest build; npm audit em 2026-07-15 |
| Cobertura backend crítica | Auth 96,92% statements/91,91% branches; Stays 97,65%/90,94%; health/cron/jobs 97,78%/92,78%; backtesting e Stripe resolver 100% | `npm run test:coverage:certified` com thresholds ≥90% |
| Frontend local | 97/97 E2E Chromium, zero skips; lint, typecheck, unitários, build de 76 rotas e budget de 103,0 KiB gzip verdes | Playwright contra Next standalone; ESLint; TypeScript; Next build em 2026-07-15 |
| Pipeline de dados | 55 testes verdes, incluindo qualidade pré-DB e ordem temporal; Ruff, format e mypy verdes, com uma exceção `PLR0913` localizada e justificada | `urban-pipeline-main/.venv/Scripts/python -m pytest`; `uv run poe lint` |
| Coletores | 109 testes verdes após contrato fail-closed, proveniência e consistência temporal/timezone | `urban-webscraping-main/.venv/Scripts/python -m pytest`; Ruff verde |
| Migrations | 45/45 entidades cobertas; 49/49 arquivos com `down()`; zero problema estrutural | `npm run audit:migrations:strict` |

## Registro de execução

| ID | Status | Entrega/evidência | Dependência restante |
|---|---|---|---|
| DOC-01 | concluído | governança aplicada; raiz reduzida de 35 para 5 entrypoints Markdown; 9 documentos ativos redistribuídos; 21 snapshots preservados com banner `SUPERSEDED`; design system consolidado; 40 DOCX arquivados e 3 jurídicos/LGPD ativos; consumidores recalculados e gate de links verde | revisão visual dos DOCX depende de LibreOffice; manter o gate documental no CI |
| DATA-Q01 | concluído | `BaseCollector` valida nome, data, fonte e coordenadas; rejeita proveniência divergente, datas inválidas, timezone inconsistente e intervalos invertidos; runner exige mínimo de fontes e degrada `no_data`; 109 testes verdes | expandir schema/proveniência até o backend e fontes reais |
| OPS-01 | concluído em produção | readiness protegido configurado; chamada anônima falha com 401 e chamada autorizada retorna 200 com DB/Redis `ok`; nenhuma credencial foi registrada na evidência | conectar monitor externo ao secret store e acumular janela de SLO |
| OPS-03 | em validação externa | Upptime/GitHub Pages publicado, três sinais públicos em 200 e CNAME correto; diagnóstico confirma domínio válido, servido pelo Pages, sem erro CAA e elegível para HTTPS; nova build solicitada em 2026-07-22 | aguardar o certificado existir, habilitar `https_enforced` e validar TLS estrito/HTTP 200 |
| OPS-04 | bloqueado por Railway/DNS | origins nativos de backend e frontend respondem 200; o frontend recuperou associação pública e região, mas continua em `Waiting for DNS update`; o backend perdeu a associação pública visível e possui região inválida bloqueando deploys; status page ainda não possui certificado customizado | corrigir a região do backend e confirmar a associação existente sem duplicar domínio/DNS; depois validar TLS, health e E2E; só habilitar proxy onde previsto após certificado válido |
| OPS-05 | concluído | Railway autenticado no workspace correto; projeto, ambiente e serviços validados antes do provisionamento | manter procedimento de acesso no cofre/handoff e revisar permissões periodicamente |
| OPS-06 | bloqueado por confirmação | duas mudanças pendentes de `Service Domain` foram inspecionadas em produção; a aplicação redeployaria o frontend e os valores completos não ficaram verificáveis na UI; nada foi aplicado ou descartado | reconciliar hostnames completos com a matriz canônica e executar smoke/rollback em janela controlada; ver [`evidence/railway-production-staged-changes-2026-07-20.md`](evidence/railway-production-staged-changes-2026-07-20.md) |
| SEC-01/02 | descoberta confirmada | auditor path-only reproduzível encontrou 11 referências históricas a dump/SQL/e-mail/scripts/`credentials.py`, sem ler conteúdo | revisar material, responder ao incidente, rotacionar e reescrever histórico de forma coordenada |
| QA-DATA | concluído nesta rodada | pipeline 55/55 com gate pré-DB de schema, tipos, nulos, duplicatas, faixas, proveniência e ordem temporal; coletores 109/109 com contrato fail-closed; relatórios não expõem valores | drift/backtest por coorte ainda exige dados históricos versionados, feature availability, modelo, target e horizonte |
| DATA-DEBT-02 | concluído | pipeline: gate Ruff verde, mypy 24→0, format 23/23 e pytest 55/55; sem `type: ignore` ou supressão global | uma exceção `PLR0913` é localizada no arquivo e preserva API pública estável |
| DATA-DEBT-03 | concluído | webscraping: format 31 pendências→0 (60/60), Ruff permaneceu zero, mypy 76 erros/30 arquivos→0/0 e pytest 109/109; assinaturas Scrapy preservadas | manter canaries reais por fonte e acompanhar stubs externos de `dateparser`/`dateutil` |
| F0-FRONT | concluído e integrado | footer/bottom-nav, fallback de imagem com timeout, cookie layout, imports mortos, Unicode e hidratação do seletor de tema corrigidos; build de 76 rotas; 21/21 regressões e estresse 5/5 do toggle verdes em servidor de produção | ampliar cobertura visual para a matriz completa de dispositivos |
| SEC-03/F1-CI | concluído no código | scanner com self-test 12/12, Gitleaks, auditoria de 467 arquivos/162 links, hook pre-commit ativo, CodeQL JS/Python e guard de backup do fork | observar primeiro CodeQL e tornar checks obrigatórios após baseline verde |
| SEC-04/LGPD | controles existentes comprovados | auditor 53/53 e self-test 5/5; 27 relações TypeORM com `User` têm `CASCADE`/`SET NULL` explícito; 58/58 testes de self-delete, RBAC, leitura owner/admin, consentimento Stays e migration verdes; build aprovado | não há exportação consolidada, anonimização ou retenção automatizadas; executar drill em banco temporário, aprovar matriz jurídica e coordenar subprocessadores antes de certificar 10/10 |
| F0/F1-BACK | concluído e validado em produção | liveness 200; readiness anônimo 401 e autorizado 200; MySQL e Redis `ok`; contrato HTTP/OpenAPI; suíte completa 92/92 e 896/896 testes, lint e build verdes; auditor estrutural de migrations | configurar monitor externo e executar matriz dinâmica/multi-réplica em staging |
| SEC-AUTH-RUNTIME | concluído | registro bloqueia mass assignment e fixa papel host; rotação de refresh token usa atualização atômica e revoga família em reuso; mutação de propriedade é owner-scoped e falha antes de provider/quota; testes de regressão incluídos | repetir matriz dinâmica com dois hosts e um admin em staging real |
| SEC-SSRF | concluído no código | importação Airbnb aceita somente HTTPS/host allowlisted, valida DNS/IP a cada salto, bloqueia redes privadas, limita redirects, tempo e tamanho; 25/25 testes focados verdes | validar contrato com sandbox/provedor e telemetria de bloqueios em staging |
| SEC-PUSH | concluído no código | Web Push valida HTTPS/DNS/IP no cadastro e novamente antes do envio; bloqueia redes privadas/metadata, DNS misto e 3xx sem restringir FCM/Mozilla/Apple; 29 testes | pinning integral do IP exigiria trocar o transporte `fetch`; manter monitoramento e revalidar em staging |
| SEC-EMAIL | concluído localmente | respostas públicas indistinguíveis, status JWT owner-scoped, `crypto.randomInt`, HMAC versionado sem plaintext novo, expiração/tentativas/lockout e migration reversível | storage de throttle continua local; validar multi-réplica/Redis e e-mail real em staging |
| SEC-HEADERS | concluído em modo observação | Next remove `X-Powered-By` e entrega HSTS, nosniff, X-Frame-Options, Referrer/Permissions/COOP/CORP e CSP Report-Only; teste de comportamento verde | observar relatórios da CSP antes de promover para enforcement sem quebrar Stripe, mapas ou Sentry |
| INPUT-DTO | concluído | gate AST canônico comprova 61/61 `@Body` em 36 controllers com classe DTO runtime; inline/`any`/interface/array raw são rejeitados; CI executa `audit:body-dtos` | manter o gate obrigatório e revisar limites sempre que contratos evoluírem |
| F2-BACK-01 | concluído | 9 rotas de billing/finanças extraídas para controller dedicado; 20 testes e build verdes | continuar decomposição incremental dos demais contextos |
| F2-BACK-02 | concluído | 15 rotas de quality/dataset/jobs extraídas para controller dedicado; guards, roles, throttles e ordem de pipeline preservados; controller monolítico reduzido para 15.929 bytes | continuar decomposição incremental de serviços e demais contextos |
| F2-BACK-03 | concluído | `AdminStaysHealthService` extraído com DI e contrato dedicado; `AdminService` 2.800→2.730 linhas; 3 suítes/18 testes e build verdes; rotas/DTOs/guards intactos | continuar decomposição incremental dos demais contextos read-only antes de mutações de alto risco |
| F2-BACK-04 | concluído | normalização read-only de metadados Airbnb extraída; `propriedade.service.ts` 2.705→2.603 linhas; 2 suítes/7 testes e build verdes; ownership/persistência intactos | continuar extrações coesas sem alterar o fluxo de importação real |
| F2-BACK-05 | concluído | projeção read-only de heatmap extraída; `event-intelligence.service.ts` 2.371→2.278 linhas; 2 suítes/9 testes e build verdes; locks/transações/persistência intactos | manter decomposição incremental em torno de projeções e scoring com contratos |
| F2-BACK-06 | concluído | resolução pura de alvos de portfólio extraída; `host-panels.service.ts` 1.856→1.759 linhas; 3 suítes/12 testes e build verdes; ownership/queries/respostas intactos | continuar por use case sem transformar a modularização em big bang |
| F2/F4-API | concluído no código | OpenAPI publica contrato SemVer `1.0.0` e estratégia `stable-unprefixed`, preservando todas as URLs; gate determinístico compara 12 operações críticas, parâmetros, bodies, respostas, segurança e 4 schemas transitivos com baseline v1 versionada | ampliar progressivamente o snapshot para 100% dos caminhos críticos e medir depreciações antes de qualquer remoção |
| F2-CRON-STRICT | concluído no código | inventário estrito aprovou 21/21 jobs com timezone, `waitForCompletion`, `AdminJobRun` e idempotência; lock consultivo MySQL e fallback local preservam semântica; self-test 8/8, testes e build verdes | validar concorrência multi-réplica, atraso e recuperação com MySQL/Redis reais em staging |
| F2-DESIGN | fundação e não-regressão concluídas | tokens, schema e gerador determinístico; baseline por arquivo bloqueia aumento ou deslocamento de 441 cores cruas, 3.109 inline styles e 43 breakpoints não canônicos; self-test 6/6 e catálogo estático de 12 componentes | reduzir a baseline gradualmente e ampliar catálogo/estados sem big bang |
| F2-PERF | concluído nesta onda | frontend shared caiu de 225,1 para 103,0 KiB gzip (-54,2%), superando a meta ≤180; Sentry lazy preserva erros/tracing e remove Replay; budget faz parte do build. Dashboard caiu de 410,31 para 64,52 kB gzip inicial, também com budget | monitorar Web Vitals reais e otimizar chunks específicos por rota sem regredir observabilidade |
| SEC-DEPS | concluído na superfície segura | backend/frontend de produção/dashboard/KNN/pipeline com zero vulnerabilidades; Next foi fixado em `15.5.21` após o advisory alto `GHSA-m99w-x7hq-7vfj`, com instalação limpa, audit zero, typecheck, build e budget de 103,1 KiB gzip verdes; overrides restritos preservam UUID do NextAuth e PostCSS sem `force`; scraping caiu de 33 para 1 | Scrapyd 1.6 exige `setuptools<81`, enquanto a correção pede ≥83; acompanhar release/substituição sem quebrar o runtime |
| QA-REPO | concluído e observado remotamente | scanner HEAD, auditor de links, auditor de histórico e workflows permanecem verdes; no PR canônico #19, todos os checks passaram no SHA `a1991a1`, incluindo build, CodeQL, migrations, E2E público, autenticado, produto, enterprise e suíte mockada | manter o PR em draft até os gates externos de DNS/TLS e DR fecharem; depois tornar checks críticos obrigatórios na `main` quando houver permissão administrativa |
| QA-FINAL | concluído localmente | backend 92 suítes/896 testes e cobertura crítica ≥90%; frontend 97/97 E2E, zero skips, unitários/lint/typecheck e build Next 76/76 verdes; shared 106 kB Next/103,0 KiB gzip; pipeline 55 e scraping 109 testes; gates de segurança, DTOs, docs, DR, authz, observabilidade, cron, LGPD, OpenAPI e migrations verdes | certificação integral continua dependente dos ensaios reais listados no handoff |
| F3-A11Y | concluído localmente | auditorias Axe em público, host e admin, desktop/mobile, teclado, foco preso/restaurado, touch 44 px, loading contextual e skip links passam dentro da suíte 97/97; zero violação `serious`/`critical` | validar zoom 200%, leitores de tela e matriz Safari/Firefox/dispositivos reais em staging |
| F2-FRONT-DEBT-01 | concluído | onboarding componentizado sem redesign: página 2.203→1.455 linhas (-34%); visual e domínio extraídos; 6/6 unitários, 8/8 E2E, lint/typecheck/build/budget verdes | continuar extrações incrementais nas páginas/admin e service API ainda >1.000 linhas |
| F2-FRONT-DEBT-02 | concluído | Event Radar admin 2.489→732 linhas (-70,6%); domínio, resumo, geo e detalhe extraídos; 6 unitários, 5/5 E2E, lint/typecheck/build/budget verdes | modularizar o client API admin e demais páginas críticas sem big bang |
| F2-FRONT-DEBT-03 | concluído | client API admin 2.596→1.585 linhas (-38,9%); Event Radar isolado com reexports compatíveis; contratos 5/5, domínio 6/6, E2E 5/5 e build/budget verdes | continuar separação por domínio mantendo módulos dedicados coesos e contratos públicos estáveis |
| F2-FRONT-RESILIENCE | concluído | boundaries recuperáveis para público/host/admin/global, Sentry e reset acessível; service worker v4 com update coordenado e invalidação seletiva; testes 4/4+4/4, PWA E2E 3/3, build 76/76 | validar install/update/offline/push em aparelhos e Safari/Firefox reais |
| F1-CI-AUX | concluído e observado remotamente | job valida instalação limpa, audit, build/budget do dashboard e audit/sintaxe do KNN; CI e release gate passaram no repositório canônico e no fork após o merge | manter o guard do fork e investigar qualquer regressão remota futura |
| F3-SLO | concluído no código; monitor público ativo | probe HTTP(S) reproduzível mede disponibilidade e p50/p95/p99, aceita readiness autenticado por variável e possui self-test local 12/12; Upptime monitora site, app e liveness da API; produção validada com liveness 200 e readiness autorizado 200 | concluir TLS do domínio de status e acumular janela real antes de certificar 99,9% |
| OBS-SEC | concluído no código | Sentry sem PII padrão, metadata de environment/release e `beforeSend` redactor; request-id validado e correlacionado no header/Sentry; conclusão HTTP gera log JSON sem payload; impressão de objeto de propriedade removida; gate e testes locais adicionados | executar smoke em staging e comprovar evento, release, correlação e alerta no projeto Sentry real sem inserir PII |
| SEC-AUTHZ | concluído no código | gate AST inventaria 103 rotas mutáveis e 106 administrativas; exige JWT ou autenticação de máquina, RBAC explícito no admin, throttle global/local, vínculo a `req.user` e allowlist pública exata de 14 rotas; self-test cobre bypass público, RBAC ausente, IDOR e throttle; logout recebeu limite local | executar matriz dinâmica em staging com dois hosts e um admin para comprovar 401/403/404, não alteração cross-tenant e 429 sem prejudicar webhook Stripe |
| F4-BILLING-LOCAL | concluído localmente | webhook preserva raw body, falha fechado em assinatura, deduplica 50 IDs recentes por pagamento e rejeita evento anterior ao watermark; metadata inválida de ciclo/quantity ganhou fallback seguro; status, checkout, quota e sync-check têm cobertura mockada; migration reversível adiciona estado de replay/ordenação | aplicar migration antes do código quando deploy for desacoplado; usar Stripe sandbox/Test Clock para retries, proration, transições temporais, reenvio assinado e concorrência entre réplicas; exactly-once distribuído e retry após falha interna ainda exigem ledger/lock transacional e política dead-letter |
| F4-STAYS-LOCAL | concluído localmente | 49 testes sem credenciais/rede cobrem consentimento versionado, preview e guardrails, validação de entrada, idempotência sequencial e corrida por chave única, retry de timeout/408/425/429/5xx com `Retry-After`, rejeição 409 sem retry, auditoria de erro, rollback owner-scoped e kill switch relido durante o lote | validar contrato e ciclo push/rollback em sandbox Stays; provar concorrência multi-réplica com MySQL real e suporte do provedor à idempotência; configurar alertas e política de reprocessamento/dead-letter |
| F3/F4-RESILIENCE | proteção off-site e restore aprovados; auditabilidade bloqueada | run `29828809180`: 18/18 tabelas, RPO 22.391 s, RTO 33 s, `AES256`, versionamento `Enabled` e lifecycle 90 dias atuais/30 dias não atuais/multipart 7 dias; `admin_audit_logs` vazio reprovou o quinto check. O writer redige segredos, tenta três vezes e falha explicitamente | publicar a correção, provar uma gravação administrativa real autorizada, gerar novo backup e repetir até 5/5; executar rollback; garantia atômica completa ainda requer outbox/transação; ver [`evidence/restore-drill-2026-07-20.md`](evidence/restore-drill-2026-07-20.md) |
| QA-KNN | concluído nesta rodada | 5/5 testes de contrato cobrem autenticação fail-closed e validação de entrada; audit sem vulnerabilidades | provar ausência de tráfego e retirar o serviço legado de forma coordenada |
| ARCH-KNN | gate estático concluído | `audit:knn-legacy:head` e worktree encontram zero consumidor executável; self-test 9/9; CI bloqueia diretório, pacote, endpoint, URL, env ou alvo de deploy que reconecte o standalone; Dockerfile/start continuam preservados | Railway/telemetria precisam provar serviço, configuração e zero tráfego por janela acordada antes de desprovisionar/remover |

## Dívida estrutural medida

Os maiores arquivos confirmam que a modularização deve ser incremental e orientada por testes:

- frontend: `service/api/admin.ts` caiu 2.596→1.585 (-38,9%), `admin/event-radar/page.tsx` 2.489→732 (-70,6%) e `onboarding/page.tsx` 2.203→1.455 (-34%); o novo módulo coeso `event-radar.ts` tem 1.015 linhas e permanece candidato a subdivisão futura;
- backend: `admin.service.ts` 2.800→2.730, `propriedade.service.ts` 2.705→2.603, `event-intelligence.service.ts` 2.371→2.278 e `host-panels.service.ts` 1.856→1.759; todos seguem acima de 1.000 linhas, agora com extrações testadas e plano incremental;
- UI: baseline versionada de 441 cores cruas, 3.109 estilos inline e 43 breakpoints não canônicos; qualquer aumento, dívida movida ou novo arquivo com débito falha no gate.
- performance: frontend compartilhado em 106 kB no relatório Next (103,0 KiB gzip medidos em instalação limpa, meta 180); dashboard entrega entrypoint de 64,52 kB gzip e Phaser assíncrono de 345,14 kB gzip;
- testes: backend completo passou 92/92 suítes e 896/896 testes; módulos críticos superam 90% em statements e branches; frontend E2E passou 97/97 cenários contra o artefato standalone, sem `skip` ou `fixme`.

## Regra de fechamento

Nenhum item com credencial ausente, aceite jurídico, DNS, integração paga, beta humano ou duas semanas de SLO será marcado como concluído por inferência. O registro deve apontar a evidência executável e a data da última validação.
