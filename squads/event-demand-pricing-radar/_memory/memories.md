# Squad Memory: Event Demand Pricing Radar

## Estilo de Escrita

## Design Visual

## Estrutura de Conteudo

## Proibicoes Explicitas

## Tecnico (especifico do squad)

- 2026-05-22: Tais criou o contrato v0 em `docs/contracts/event-radar-contract-v0.md`, fixtures em JSON/Playwright e uma spec backend de contrato. Os E2E de `Urban-front-main/e2e/event-radar.spec.ts` usam mocks contratuais e pulam com motivo explicito enquanto `/events`, `/event-radar` ou o radar admin ainda nao existirem.
- 2026-05-22: Lia implementou a fundacao backend P0 para Event Intelligence com entidades `event_intelligence_snapshots`, `event_property_impacts` e `pricing_decision_snapshots`, modulo `event-intelligence`, endpoints host/admin reais e fallback explicito via `AnalisePreco` enquanto o motor da Nico nao persiste curvas/scores finais.
- 2026-05-22: Maya criou a experiencia host P0 em `/events`, `/events/[eventId]` e `/event-radar`, com componentes `event-intelligence`, bloco host no `api.ts`, fallback mockado marcado em `hostEventRadarMocks.ts`, sidebar host atualizada e validacao por `tsc --noEmit` + ESLint direcionado. O dev server Next ficou preso em `Starting...` sem abrir a porta 3000 nesta maquina.
- 2026-05-22: Nico entregou `EventPricingIntelligenceService` com score de demanda, score de captura do imovel e curva de absorcao de preco em quatro cenarios. O motor roda de forma deterministica e tem cobertura em `event-pricing-intelligence.service.spec.ts`.
- 2026-05-22: Otto entregou a tela admin `/admin/event-radar`, contratos/adapters admin em `api.ts`, entrada no `AdminShell` e CTA a partir de `/admin/events`. A tela usa fallback contratual quando a API ainda nao responde.
- 2026-05-22: Integracao final conectou `EventIntelligenceService` ao motor da Nico: catalogo/heatmap usam demanda derivada quando nao ha snapshot; impactos derivados de `AnalisePreco` agora recebem `propertyCaptureScore`, curva de absorcao, probabilidade de reserva, receita esperada e cenarios. `simulate-pricing` retorna `ok` quando ha curva calculada e mantem stub apenas quando faltam dados. Checks finais: backend `tsc --noEmit`, frontend `tsc --noEmit` e Jest de motor/contrato/radar passaram.
- 2026-05-22: Backend Persistence evoluiu os recomputes admin para persistencia real v0: `POST /admin/events/:eventId/recompute-intelligence` e batch salvam `EventIntelligenceSnapshot` append-only, salvam `EventPropertyImpact` a partir de `AnalisePreco` persistivel e retornam contadores reais em `writes`. `pricing_decision_snapshot` segue lacuna explicita. Checks: backend typecheck e Jest de event-intelligence/motor/contrato passaram.
- 2026-05-22: Tais consolidou QA Release com P0 em ~69% e meta ~85%. Evidencias desta rodada: Playwright descobriu 4 testes em `event-radar.spec.ts`, fixture JSON parseou com 2 eventos/2 grupos de impacto/1 curva e Jest `event-radar-contract.spec.ts` passou com 5 testes. Lacunas prioritarias: persistir snapshots/impactos, auditar `pricing_decision_snapshot`, rodar E2E sem skips, validar visual desktop/mobile e feature flags.
- 2026-05-22: Status consolidado salvo em `docs/status-entregas-radar-eventos-2026-05-22.md`. Apos Backend Persistence e Pricing Audit, estimativa atual e ~82% no P0 tecnico e ~75-78% para release controlado com confianca de produto; roadmap total P0-P2 segue em ~43% porque P2 depende de outcomes reais, auto-apply e calibracao.
- 2026-05-22: Pricing Audit preparou a trilha auditavel de `pricing_decision_snapshots` sem tocar `event-intelligence/`: tipou `inputSignals`/`guardrails`/`outcome`, adicionou builders em `PricingCalculateService` para draft de decisao e patch de outcome, criou spec focada e registrou handoff em `squads/event-demand-pricing-radar/output/pricing-audit-handoff-2026-05-22.md`. Lacuna: persistencia real deve ser conectada pela frente Backend Persistence.
- 2026-05-22: Rodada multiagentes de continuidade concluiu cinco frentes. Chandrasekhar conectou a persistencia real de `PricingDecisionSnapshot` no recompute de `EventIntelligenceService`; Pasteur poliu Host em `/events`, `/events/[eventId]`, `/event-radar` e componentes compartilhados; Descartes poliu `/admin/event-radar`; Aristotle removeu skips do E2E e atualizou fixtures/checklists; Boyle criou runbook de idempotencia/retry/rollout em `docs/runbooks/event-intelligence-recompute-idempotency-2026-05-22.md`.
- 2026-05-22: Validacoes apos a rodada: backend `tsc --noEmit` verde; Jest backend direcionado verde com 5 suites/23 testes; frontend `tsc --noEmit` verde; ESLint direcionado Host/Admin verde; Playwright `event-radar.spec.ts --list` lista 4 testes e nao ha `test.skip` condicional. Dev server Next ainda ficou preso em `Starting...`, entao falta Playwright real em browser local/staging.
- 2026-05-22: Status atualizado para ~88% no P0 tecnico, ~82% para release controlado e ~45% no roadmap total P0-P2. As lacunas remanescentes sao staging/DB real, idempotencia implementada em codigo, fila/retry, heatmap geo rico e conexao de outcomes/PriceUpdate ao aprendizado de pricing.
- 2026-05-22: Rodada final release concluiu cinco frentes. Sagan implementou idempotencia defensiva de `PricingDecisionSnapshot` com `idempotencyKey`/`signalsHash` em `inputSignals`; Anscombe fortaleceu `criarPatchOutcomeSnapshotDecisao` para aceite/aplicacao/rejeicao/reserva/receita/fonte; Dalton evoluiu heatmap Host com regioes/cidades/imoveis expostos/eventos sem geo; Hypatia evoluiu Geo Ops Admin com hotspots/gaps/sem geo/receita/blind spots; Banach criou runner `test:e2e:event-radar` e runbook de release gate.
- 2026-05-22: Validacoes integradas da rodada final: backend `tsc --noEmit` verde; Jest direcionado verde com 5 suites/27 testes; frontend `tsc --noEmit` verde; ESLint direcionado Host/Admin/runner verde; `node scripts/event-radar-release-gate.mjs --list` listou 4 testes; `event-radar.spec.ts` segue sem skip. Playwright real continua bloqueado por ambiente local `.next`/processos: porta nova fica em `Starting...` e servidor 3007 retorna HTTP 500.
- 2026-05-22: Status atualizado para ~93% no P0 tecnico, ~87% para release controlado e ~50% no roadmap total P0-P2. Para 100% faltam Playwright 4/4 verde em staging/CI limpo, smoke DB real/migrations, lock/fila/retry operacional, persistencia de outcomes reais e evidencia visual desktop/mobile.
- 2026-05-22: Rodada Playwright Fix isolou a causa final. Apos parar Next concorrente e limpar `.next`, o Next independente em `127.0.0.1:3041` ficou `Ready`; `/events`, `/events/evt-gp-sp-2026`, `/event-radar` e `/admin/event-radar` responderam HTTP 200; browser Playwright direto abriu `/events` com HTTP 200. O bloqueio residual do `@playwright/test` ocorreu em `clear output`/`apply rebaselines` ao usar `Urban-front-main/test-results` dentro do OneDrive.
- 2026-05-22: Runner `scripts/event-radar-release-gate.mjs` foi ajustado para gravar artefatos Playwright fora do OneDrive por padrao (`C:\tmp\urban-ai-event-radar-playwright`) e aceitar `--output`/`E2E_OUTPUT_DIR`. Status revisado para ~95% P0 tecnico e ~90% release controlado. Reexecucao final do gate com output externo nao ocorreu por limite de uso do ambiente Codex.
- 2026-05-22: Rodada multiagentes Playwright 100 concluiu auditoria final do runner, criterio de evidencia 100 e smoke direto `scripts/event-radar-direct-smoke.mjs`. O smoke direto usa Playwright como biblioteca, roda desktop/mobile por padrao, falha em `pageerror`, checa ausencia de skips no spec e escreve evidencia em `C:\tmp\urban-ai-event-radar-direct-smoke`. Status honesto: 95% comprovado, ~98% pronto para execucao; 100% depende de rodar o gate oficial 4/4 ou o smoke direto com exit code 0 em ambiente com Node/Playwright liberado.
- 2026-05-23: QA Release Gate fechou o Event Radar browser gate com `npm run test:e2e:event-radar -- --no-server --base-url http://127.0.0.1:3041 --request-timeout-ms 120000` retornando 4/4 Playwright verde e smoke direto desktop/mobile verde contra Next temporario em 3043. Ajustes: E2E e smoke evitam falso negativo por `<option>` oculto/duplicacao responsiva; runner diagnostica encerramento precoce do Next e `ENOSPC`. Ressalva: staging/DB real ainda pendentes e ambiente local pode falhar por `ENOTEMPTY`, `EPERM` em `.next\trace` ou falta de espaco.
- 2026-05-23: Backend DB & Recompute adicionou lock/retry idempotente em `EventIntelligenceService`: `jobRunId` e reutilizado em tentativas internas, snapshots usam dedupe `jobRunId + eventId`, impacts usam dedupe `jobRunId + eventId + propertyAddressId + analisePrecoId`, MySQL real usa advisory lock via `QueryRunner` e o response expõe `runtime` + contadores `writes.created/reused/skipped`.
- 2026-05-23: Frente Outcomes & Learning Loop conectou `PriceUpdate`/Stays a `pricing_decision_snapshot.inputSignals.outcome`, criou `PricingOutcomeLearningService` para dataset/calibracao de absorcao, adicionou calibracao opcional em `priceAbsorptionCurve` e validou Jest focado 4 suites/37 testes + typecheck backend.
- 2026-05-23: Noether Outcomes Calibration adicionou criterio objetivo de prontidao em `PricingOutcomeLearningService.evaluateCalibrationReadiness(...)`, script dry-run/fixture `scripts/pricing-outcome-calibration-report.ts`, spec focada e handoff `outcomes-calibration-readiness.md`. Validado sem DB real/producao com Jest 1 suite/4 testes e dry-run do relatorio.
- 2026-05-23: Frente Heatmap Geo & Experience Closure elevou UX Host/Admin sem backend/e2e: Host Radar normaliza `h3Index`/`geohash`/bbox e deriva celulas por geohash quando o backend nao entrega heatmap; `/events` usa lat/lng reais no mapa e lista eventos sem geo; Admin Geo Ops ganhou mapa operacional compacto com celulas, hotspots, gaps e codigos geo. Typecheck e ESLint direcionado passaram; dev server local ainda reproduziu problema ambiental/cache em `.next/routes-manifest.json`. Handoff salvo em `squads/event-demand-pricing-radar/output/2026-05-23-roadmap-closure/heatmap-geo-experience.md`; percentual recomendado da frente: 95%, condicionado a evidencia browser/staging para 100%.
- 2026-05-23: Frente Auto-Apply & Guardrails adicionou cohort seguro `event-safe-beta` ao `stays-auto-apply`: push real exige allowlists live, consentimento Stays, baseline de rollback, `PricingDecisionSnapshot`, confidence/probabilidade/multiplicador seguros, preco auditado e ausencia de risk flags criticas. Specs focadas passaram com 1 suite/11 testes e typecheck backend verde. Handoff salvo em `squads/event-demand-pricing-radar/output/2026-05-23-roadmap-closure/auto-apply-guardrails.md`; percentual recomendado da frente: 72%, pendente de smoke Stays/DB real e rollback real.
- 2026-05-23: Turing Auto-Apply Beta Ops fechou a camada operacional event-safe beta sem rodar producao: `.env.example` agora explicita os envs seguros do auto-apply, o runbook `docs/runbooks/stays-beta-private-smoke.md` reforca dry-run/allowlists/consentimento/rollback e o handoff foi salvo em `squads/event-demand-pricing-radar/output/2026-05-23-roadmap-closure/auto-apply-beta-ops.md`. Status recomendado: 78% da frente, liberado apenas para dry-run staging/sandbox; live depende de smoke real assistido e rollback comprovado.
- 2026-05-23: Lovelace Frontend/Staging Gate confirmou que o Event Radar parametriza app remoto por `--base-url`/`E2E_BASE_URL` e API por `NEXT_PUBLIC_API_URL`; o runner oficial agora aceita `--api-url`/`E2E_API_URL` e propaga para `NEXT_PUBLIC_API_URL` quando sobe Next local. Checks: `node --check` dos dois scripts verde, busca por skips sem ocorrencias e `--list` com 4 testes. Handoff salvo em `squads/event-demand-pricing-radar/output/2026-05-23-roadmap-closure/frontend-staging-gate.md`; percentual recomendado da frente: 92%, pendente de execucao real contra staging.
